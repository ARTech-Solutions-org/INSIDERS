{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && cd artifacts/admin-app && pnpm run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
